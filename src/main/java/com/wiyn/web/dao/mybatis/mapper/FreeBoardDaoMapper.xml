<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wiyn.web.dao.FreeBoardDao">

<select id="getBoard" resultType="com.wiyn.web.entity.FreeBoard">

		SELECT id,
		title,
		contentSrc,
		content,
		hit,
		regDate,
		memberId,
		FROM FreeBoard
		WHERE id=#{id}
</select>

<insert id="add" parameterType="com.wiyn.web.entity.FreeBoard">
	<selectKey order="BEFORE" keyProperty="id" resultType="String">
		select max(cast(id as unsigned))+1 id from FreeBoard
	</selectKey>

	INSERT INTO FreeBoard
	(`id`,
	`title`,
	`contentSrc`,
	`content`,
	`regDate`,
	`memberId`)

	VALUES
	(
	#{id},
	#{title},
	#{contentSrc},
	#{content},
	now(),
	#{memberId}
	)
	
</insert>

</mapper>